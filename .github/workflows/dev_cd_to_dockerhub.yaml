name: CD - Dev to DockerHub

on:
    workflow_run:
        workflows: ["CI - Dev Branch Testing"]
        types:
            - completed

jobs:
    build-and-push:
        name: Build and Push Docker Images to DockerHub
        runs-on: ubuntu-latest

        # Only run if the CI workflow was successful
        if: ${{ github.event.workflow_run.conclusion == 'success' }}

        # Define services
        strategy:
            fail-fast: false # Continue other jobs even if one fails
            matrix:
                service:
                    - name: sproutvrschool-backend-server
                      context: .
                      dockerfile: SproutVRSchool.Presentation/Dockerfile
                    
        steps:
            - name: Checkout Repository Code
              uses: actions/checkout@v4
              with:
                  ref: ${{ github.event.workflow_run.head_sha }}

            # Step 1: Docker builds
            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v2

            # Step 2: Log in to DockerHub
            - name: Log in to DockerHub
              uses: docker/login-action@v2
              with:
                  username: ${{ secrets.DOCKER_USERNAME }}
                  password: ${{ secrets.DOCKER_PASSWORD }}

            # Step 3: Docker metadata for image (Tags)
            - name: Define Image Tags and Labels (Dev)
              id: meta
              uses: docker/metadata-action@v5
              if: success()
              with:
                  images: ${{ secrets.DOCKER_USERNAME }}/${{ matrix.service.name }}
                  tags: |
                      type=raw,value=dev

            # Step 4: docker build and push
            - name: Build & Push ${{ matrix.service.name }}
              uses: docker/build-push-action@v6
              if: success()
              with:
                  context: ${{ matrix.service.context }}
                  file: ${{ matrix.service.dockerfile }}
                  push: true
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}
